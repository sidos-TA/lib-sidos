import { styled } from "styled-components";

const FieldWrapper = styled.div`
  .ant-input {
    padding: 12px;
    font-size: 18px;
  }
  .ant-select-selector {
    padding: 12px;
    font-size: 18px;
  }
`;

export default FieldWrapper;
